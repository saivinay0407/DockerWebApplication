version: "3.9"
services:

  devopsdb:
    image: venkatasaivinay/db:v1
    ports:
      - "3306:3306"
    deploy:
      replicas: 1
      restart_policy:
        condition: on-failure
    networks:
      - application_network
  application:
    image: venkatasaivinay/application:v1
    ports:
      - "1111:80"
    deploy:
      replicas: 2
      restart_policy:
        condition: on-failure
    networks:
      - application_network
    depends_on:
      - devopsdb

networks:
  application_network:
    driver: overlay
